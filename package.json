{
  "name": "yangi-choy",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "web": "turbo dev --filter=web",
    "mobile": "turbo dev --filter=mobile"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
